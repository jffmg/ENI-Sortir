{% extends 'base.html.twig' %}

{% block title %}Sortir.com | Administration{% endblock %}

{% block main %}

    <h1 class="app-main-title">Portail d'administration</h1>

    <div class="app-framed">
        <a class="app-pink-label" href="{{ path('admin_addParticipants') }}">Ajouter des participants</a>
    </div>
    <div class="app-framed">
        <a class="app-pink-label" href="{{ path('admin_manager') }}">Gérer les participants</a>
    </div>
    <div class="app-framed">
        <a class="app-pink-label" href="{{ path('admin_cities') }}">Gérer les villes</a>
    </div>
    <div class="app-framed">
        <a class="app-pink-label" href="{{ path('admin_campuses') }}">Gérer les campus</a>
    </div>
{% endblock %}